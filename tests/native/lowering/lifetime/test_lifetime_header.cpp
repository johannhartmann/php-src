#include "Zend/Native/Lowering/StraightLine/zend_mir_straight_line.h"

static_assert(ZEND_MIR_STRAIGHT_LINE_OPCODE_RETURN == 62);
static_assert(ZEND_MIR_STRAIGHT_LINE_OPCODE_FREE == 70);

int main()
{
	return 0;
}
